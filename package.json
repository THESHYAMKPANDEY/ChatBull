{
  "name": "chatbull",
  "private": true,
  "scripts": {
    "build": "cd old_version/backend && npm install && npm run build",
    "start": "cd old_version/backend && npm start"
  },
  "devDependencies": {
    "@types/dotenv": "^6.1.1",
    "@types/express-rate-limit": "^5.1.3",
    "@types/socket.io": "^3.0.1"
  },
  "dependencies": {
    "@privacyresearch/libsignal-protocol-typescript": "^0.0.16",
    "buffer": "^6.0.3",
    "events": "^3.3.0",
    "multer": "^2.0.2",
    "process": "^0.11.10",
    "socket.io-client": "^4.8.3"
  }
}
